server:
  port: 8002

eureka:
  instance:
    hostname: service-b
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/

spring:

  application:
    name: service-b

  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: service-b
      profile: dev
    bus:
      trace:
        enabled: true
      enabled: true

  rabbitmq:
      host: localhost
      port: 5672
      username: guest
      password: guest



feign:
  hystrix:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
